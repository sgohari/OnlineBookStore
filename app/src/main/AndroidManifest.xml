<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.bishnu.byn_comp304_group_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MusicService" />

        <activity android:name=".BookReview" />
        <activity android:name=".ViewWebview" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".DictionaryActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".SendSMS" />
        <activity android:name=".SendInternationalSMS" />
        <activity android:name=".SendEmail"></activity>
    </application>

</manifest>